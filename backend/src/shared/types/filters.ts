export type CourseQuery = {
  status?: string;
  search?: string;
  page?: string;
  limit?: string;
};
export type MaterialQuery = {
  type?: string;
  search?: string;
  page?: string;
  limit?: string;
};
export type LessonQuery = {
  search?: string;
  page?: string;
  limit?: string;
};
